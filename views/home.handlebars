<h1>The Tech Blog</h1>
{{#each posts as |post|}}
<div>
    <div>
        <h2>{{post.title}}</h2>
        <p>Posted by {{post.author}} on {{post.created_on}}</p>
    </div>
    <p>{{post.description}}</p>
</div>
{{/each}}